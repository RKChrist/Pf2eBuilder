<button @attributes="Attributes"
        class="pf-btn pf-btn--@(Variant.CssSuffix()) @(Hold ? "pf-btn--hold" : null) @(Busy ? "pf-btn--busy" : null) @Class"
        type="@(Type.HtmlValue())"
        disabled="@(Disabled || Busy)"
        aria-busy="@(Busy ? "true" : null)"
        @onclick="OnClick">
    @if (Hold)
    {
        <span class="pf-btn__hold" aria-hidden="true"></span>
    }
    @if (Busy)
    {
        <span class="pf-spinner" aria-hidden="true"></span>
    }
    @ChildContent
</button>

@code {
    [Parameter(CaptureUnmatchedValues = true)]
    public IReadOnlyDictionary<string, object>? Attributes { get; set; }

    [Parameter] public string? Class { get; set; }

    [Parameter] public ButtonVariant Variant { get; set; } = ButtonVariant.Secondary;

    [Parameter] public ButtonType Type { get; set; } = ButtonType.Button;

    [Parameter] public bool Disabled { get; set; }

    [Parameter] public bool Busy { get; set; }

    [Parameter] public bool Hold { get; set; }

    [Parameter] public EventCallback<MouseEventArgs> OnClick { get; set; }

    [Parameter] public RenderFragment? ChildContent { get; set; }
}
