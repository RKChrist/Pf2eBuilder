bin/
obj/
*.user
.vs/
node_modules/

# Working checklist, not part of the design record.
todo.md

# Ingest output is regenerated from the stored snapshot by `transform`.
tools/rules-import/out/
.claude/settings.local.json

# Pulled snapshot, restored by `rules-import pull`. Tracked or not is decided
# once its real size is known.
Sources/aon-snapshot/
