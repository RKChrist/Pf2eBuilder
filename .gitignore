bin/
obj/
*.user
.vs/
node_modules/

# Working checklist, not part of the design record.
todo.md

# Ingest output is regenerated from the stored snapshot by `transform`.
tools/rules-import/out/
.claude/settings.local.json


# Copied from tools/tokens/dist by the client's SyncDesignTokens target on every build.
src/Pf2e.Client/wwwroot/css/tokens.css

# Local databases, rebuilt from migrations and reseeded on startup.
*.db
*.db-shm
*.db-wal
